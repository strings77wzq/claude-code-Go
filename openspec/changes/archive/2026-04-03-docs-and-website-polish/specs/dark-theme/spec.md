## ADDED Requirements

### Requirement: Dark theme by default
The site SHALL use a dark theme by default.

#### Scenario: Dark theme
- **WHEN** a user visits the site
- **THEN** the default theme is dark with GitHub-dark color palette
