## ADDED Requirements

### Requirement: Scroll-triggered animations
Page elements SHALL animate in when scrolled into view.

#### Scenario: Fade-in on scroll
- **WHEN** a user scrolls to a feature card
- **THEN** it fades in with a subtle slide-up animation
