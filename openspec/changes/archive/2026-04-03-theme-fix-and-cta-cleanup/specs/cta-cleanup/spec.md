## MODIFIED Requirements

### Requirement: No redundant CTA
The homepage SHALL NOT have redundant call-to-action sections.

#### Scenario: No duplicate buttons
- **WHEN** a user views the homepage
- **THEN** the CTA buttons appear only in the Hero section, not repeated at the bottom
