## ADDED Requirements

### Requirement: Spinner animation
The TUI SHALL show a spinner animation while waiting for API response.

#### Scenario: Thinking state
- **WHEN** the agent is waiting for API response
- **THEN** a spinner animation is shown with "Thinking..." text
