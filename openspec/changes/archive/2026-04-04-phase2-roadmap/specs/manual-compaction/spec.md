## ADDED Requirements

### Requirement: Manual context compaction
The system SHALL support manual context compaction via user command.

#### Scenario: Compact command
- **WHEN** a user types `/compact`
- **THEN** the conversation history is compacted and a confirmation is shown
