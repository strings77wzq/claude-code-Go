## ADDED Requirements

### Requirement: LSP integration
The system SHALL support Language Server Protocol operations.

#### Scenario: LSP operations
- **WHEN** the LSP client is connected
- **THEN** it supports: symbols, references, diagnostics, definition, hover
