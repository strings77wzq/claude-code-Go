## ADDED Requirements

### Requirement: Feature blocks design
The homepage features SHALL be displayed as full-width blocks with tags.

#### Scenario: Feature blocks
- **WHEN** a user views the features section
- **THEN** each feature is a full-width block with title, description, and tags at bottom
