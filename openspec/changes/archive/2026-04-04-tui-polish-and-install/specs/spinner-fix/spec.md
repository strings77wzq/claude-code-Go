## MODIFIED Requirements

### Requirement: Spinner animation works
The TUI SHALL show a working spinner animation while waiting for API response.

#### Scenario: Thinking state
- **WHEN** the agent is waiting for API response
- **THEN** a visible spinner animation is shown with "Thinking..." text
