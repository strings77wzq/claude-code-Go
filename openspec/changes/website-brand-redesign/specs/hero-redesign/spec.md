## ADDED Requirements

### Requirement: Redesigned hero section
The homepage hero SHALL present the project identity and installation command.

#### Scenario: Hero content
- **WHEN** a user views the homepage
- **THEN** they see: project name, tagline "模型提供智能，harness 提供可靠性", install command with copy button, CTA buttons

#### Scenario: CTA buttons
- **WHEN** a user views the hero
- **THEN** they see: "Get Started" (primary), "View Docs" (secondary), "GitHub" (alt)
