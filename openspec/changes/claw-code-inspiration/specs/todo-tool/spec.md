## ADDED Requirements

### Requirement: TodoWrite tool
The system SHALL provide a TodoWrite tool for task tracking.

#### Scenario: Create todos
- **WHEN** the LLM calls TodoWrite with new todos
- **THEN** the todos are stored and displayed to the user

#### Scenario: Update todos
- **WHEN** the LLM updates todo status
- **THEN** the updated todos are reflected in the display
