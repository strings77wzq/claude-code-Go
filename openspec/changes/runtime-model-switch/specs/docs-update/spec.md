## MODIFIED Requirements

### Requirement: Documentation updates
The documentation SHALL reflect the new runtime model switching capability.

#### Scenario: Configuration guide updated
- **WHEN** a user reads the configuration guide
- **THEN** they see instructions for switching models at runtime via `/model` command

#### Scenario: Roadmap updated
- **WHEN** a user views the roadmap feature comparison table
- **THEN** "Runtime model switching" is marked as complete for claude-code-Go
